import { ContainerPreviewPositionPipe } from './container-preview-position.pipe';

describe('ContainerPreviewPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new ContainerPreviewPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
